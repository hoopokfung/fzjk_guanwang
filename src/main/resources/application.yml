spring:
  thymeleaf:
    mode: HTML
    cache: false #关掉模板引擎的缓存
  profiles:
    active: dev  #指定使用的yml文件
  messages:
    basename: i18n/login
    encoding: UTF-8
  servlet:
    multipart:
      max-file-size: 10MB
      location: F://upload/imgs/
      max-request-size: 10MB


